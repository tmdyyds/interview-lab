# Laravel

## 计划收录

- 请求生命周期（`public/index.php` → `HttpKernel` → 路由 → 响应）
- 服务容器（IoC）原理与绑定/解析流程
- 服务提供者（ServiceProvider）注册顺序
- Facade 原理与与容器的关系
- 中间件管道（Pipeline）实现
- Eloquent ORM：关联、懒加载、N+1、事件
- 队列：驱动、失败重试、`Horizon`
- 事件与广播
- 门面 vs 依赖注入的选择
- 常见性能优化（`config:cache` `route:cache` `optimize`）
